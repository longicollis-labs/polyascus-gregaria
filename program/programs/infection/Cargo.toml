[package]
name = "infection"
version = "0.1.0"
description = "Polyascus infection — the on-chain colonisation record"
edition = "2021"

[lib]
crate-type = ["cdylib", "lib"]
name = "infection"

[features]
default = []
cpi = ["no-entrypoint"]
no-entrypoint = []
no-idl = []
no-log-ix-name = []
idl-build = ["anchor-lang/idl-build"]

[dependencies]
anchor-lang = "0.31.1"
